# Core
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11          # sparse summing matrix + MinT solve (hierarchy/)

# Models
lightgbm>=4.1
xgboost>=2.0
torch>=2.1
scikit-learn>=1.3

# Config & tracking
# (hydra-core 1.3 is broken on Python 3.14 — its argparse help strings crash;
#  we compose the same YAML config tree with OmegaConf directly, see utils/config.py)
omegaconf>=2.3
mlflow>=2.9

# Data access
kaggle>=1.6

# Viz
matplotlib>=3.8
seaborn>=0.13

# Dev
pytest>=7.4
tqdm>=4.66
