"""m5forecast — hierarchical demand forecasting on the M5 dataset.

Layers (may only import leftward):
    utils <- data <- features <- models <- evaluation <- analysis
    hierarchy depends only on data; used by models/evaluation.
"""

__version__ = "0.1.0"
